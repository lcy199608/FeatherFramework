// <auto-generated>

using System.Collections.Generic;
using System.Linq;

namespace cfg
{
    public sealed class Language
    {
        private readonly Dictionary<int, LanguageInfo> _dataMap;
        private readonly List<LanguageInfo> _dataList;

        public Language(IEnumerable<LanguageInfo> rows)
        {
            _dataList = rows?.ToList() ?? new List<LanguageInfo>();
            _dataMap = _dataList.ToDictionary(row => row.Id);
        }

        public Dictionary<int, LanguageInfo> DataMap => _dataMap;
        public List<LanguageInfo> DataList => _dataList;

        public LanguageInfo Get(int key) => _dataMap[key];
        public LanguageInfo GetOrDefault(int key) => _dataMap.TryGetValue(key, out var value) ? value : null;
        public bool TryGet(int key, out LanguageInfo value) => _dataMap.TryGetValue(key, out value);
        public LanguageInfo this[int key] => _dataMap[key];
    }
}
