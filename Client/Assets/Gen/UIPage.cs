// <auto-generated>

using System.Collections.Generic;
using System.Linq;

namespace cfg
{
    public sealed class UIPage
    {
        private readonly Dictionary<int, UIPageInfo> _dataMap;
        private readonly List<UIPageInfo> _dataList;

        public UIPage(IEnumerable<UIPageInfo> rows)
        {
            _dataList = rows?.ToList() ?? new List<UIPageInfo>();
            _dataMap = _dataList.ToDictionary(row => row.Id);
        }

        public Dictionary<int, UIPageInfo> DataMap => _dataMap;
        public List<UIPageInfo> DataList => _dataList;

        public UIPageInfo Get(int key) => _dataMap[key];
        public UIPageInfo GetOrDefault(int key) => _dataMap.TryGetValue(key, out var value) ? value : null;
        public bool TryGet(int key, out UIPageInfo value) => _dataMap.TryGetValue(key, out value);
        public UIPageInfo this[int key] => _dataMap[key];
    }
}
