// <auto-generated>

using System.Collections.Generic;
using System.Linq;

namespace cfg
{
    public sealed class Item
    {
        private readonly Dictionary<int, ItemInfo> _dataMap;
        private readonly List<ItemInfo> _dataList;

        public Item(IEnumerable<ItemInfo> rows)
        {
            _dataList = rows?.ToList() ?? new List<ItemInfo>();
            _dataMap = _dataList.ToDictionary(row => row.Id);
        }

        public Dictionary<int, ItemInfo> DataMap => _dataMap;
        public List<ItemInfo> DataList => _dataList;

        public ItemInfo Get(int key) => _dataMap[key];
        public ItemInfo GetOrDefault(int key) => _dataMap.TryGetValue(key, out var value) ? value : null;
        public bool TryGet(int key, out ItemInfo value) => _dataMap.TryGetValue(key, out value);
        public ItemInfo this[int key] => _dataMap[key];
    }
}
